# fucntion-adding-two-numbers
created a function which adds two numbers taking input from the user
def add():
    a = int(input("Enter the first number:"))
    b = int(input("Enter the second number:"))
    return a+b
result = add()
print(result)
